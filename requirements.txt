# ============================================
# Đồ án: Chiến lược Giao dịch Crypto Tự động
# dựa trên Sentiment Analysis
# ============================================
# Requirements cho Streamlit Cloud (chỉ dashboard)
# Các thư viện ML/NLP nặng (tensorflow, torch, transformers...)
# chỉ cần khi train model ở local, không cần cho dashboard.

# Data (tải dữ liệu giá trực tiếp khi không có CSV)
yfinance>=0.2.31

# Visualization & Dashboard
plotly>=5.18.0
streamlit>=1.29.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Data Processing
pandas>=2.1.0
numpy>=1.24.0
